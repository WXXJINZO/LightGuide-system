# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

.yalc
yarn.lock
yalc.lock
node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
# 仅忽略仓库根的库构建产物，避免误伤 server/src/lib（IFC 内核等）
/lib
.webcad
.taskmaster
docs
tests
.claude

# LightGuide 后端 SQLite 运行时数据（schema/seed 在 server/src/db 内，数据库文件不入库）
server/data/

# 本地 HTTPS dev server 自签名证书 / 私钥（不入库，按需自行生成）
key.pem
cert.pem
